<%= form_for([@article, @article.comments.build]) do |f| %>
 <p>
    <%= f.label :Комментатор %><br>
    <%= f.text_field :commenter %>
  </p>
  <p>
    <%= f.label :Текст %><br>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>